var app = {
    // Application Constructor
    initialize: function() {
        document.addEventListener('deviceready', function() {
            console.log('are you ready too?');
        }, false);
    }
};

app.initialize();